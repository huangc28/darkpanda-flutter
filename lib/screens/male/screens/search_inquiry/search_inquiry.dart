import 'package:flutter/material.dart';

class SearchInqiury extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Search inquiry'),
    );
  }
}
