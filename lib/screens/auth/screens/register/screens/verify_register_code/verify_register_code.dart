import 'package:flutter/material.dart';

class VerifyRegisterCode extends StatelessWidget {
  const VerifyRegisterCode();

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('verify register code'),
    );
  }
}
