part of '../inquirer_profile.dart';

class InquirerDescription extends StatelessWidget {
  const InquirerDescription({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      'InquirerDescription',
      style: TextStyle(color: Colors.white),
      textAlign: TextAlign.start,
    );
  }
}
