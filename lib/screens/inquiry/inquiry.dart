import 'package:flutter/material.dart';

class Inquiry extends StatelessWidget {
  const Inquiry({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('inquiry detail page'),
    );
  }
}
